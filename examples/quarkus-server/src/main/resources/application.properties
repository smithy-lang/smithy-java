# Smithy operations are served by Quarkus's HTTP server (per ADR-0003).
# Configure host/port using the standard Quarkus HTTP keys; the bridge
# mounts Smithy operations on the same router that serves /q/health etc.
quarkus.http.port=8080

quarkus.banner.enabled=false
